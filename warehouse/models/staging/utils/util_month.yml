version: 2

models:
  - name: util_month
    description: A utility table to generate a date spline for each month between
      2015 and the current date.
    columns:
      - name: _unique
        description: Primary key
        tests:
          - unique
          - not_null
