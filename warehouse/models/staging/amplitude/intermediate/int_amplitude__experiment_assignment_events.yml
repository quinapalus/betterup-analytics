version: 2

models:
  - name: int_amplitude__experiment_assignment_events
    description: >
      Amplitude experiment assignment events.
    columns:
      - name: uuid
        description: >
          A unique identifier per row (event sent). 
        tests:
          - unique
          - not_null
