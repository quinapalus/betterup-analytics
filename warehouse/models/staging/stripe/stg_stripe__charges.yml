version: 2

models:
  - name: stg_stripe__charges
    description: Charges applied to invoices by customers
    columns:
      - name: stripe_charge_id
        description: Primary key
        tests:
          - unique
          - not_null
