version: 2

models:
  - name: int_stripe__events_invoice
    description: Events specific to invoices.
    columns:
      - name: stripe_event_id
        description: Primary key
        tests:
          - unique
          - not_null
