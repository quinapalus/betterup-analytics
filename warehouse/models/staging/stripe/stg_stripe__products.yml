version: 2

models:
  - name: stg_stripe__products
    description: Products stored in Stripe.
    columns:
      - name: stripe_product_id
        description: Primary key
        tests:
          - unique
          - not_null
