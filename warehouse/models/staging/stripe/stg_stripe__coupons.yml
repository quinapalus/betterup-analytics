version: 2

models:
  - name: stg_stripe__coupons
    description: Coupons stored in stripe.
    columns:
      - name: stripe_coupon_id
        description: Primary key
        tests:
          - unique
          - not_null
