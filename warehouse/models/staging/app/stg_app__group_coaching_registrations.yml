version: 2

models:
  - name: stg_app__group_coaching_registrations
    description: Application table containing Group Coaching Registrations in betterup-app
    columns:
      - name: group_coaching_registration_id
        description: Primary key
        tests:
          - not_null
          - unique

      - name: group_coaching_cohort_id
        description: Id of the cohort this registration is for.

      - name: user_id
        description: Id of user who has registered for the cohort.

      - name: created_at
        description: Time when the Rails object was created in UTC.

      - name: canceled_at
        description: The 'canceled_at' column represents the timestamp of when a group
          coaching registration was canceled. [GenAI Description]

      - name: updated_at
        description: Time when the Rails object was updated in UTC.
