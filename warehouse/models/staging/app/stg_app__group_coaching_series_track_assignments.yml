version: 2

models:
  - name: stg_app__group_coaching_series_track_assignments
    description: This DBT model tracks assignments of individual users to group coaching
      series tracks. It is composed of data from the parent models "group_coaching_series_track_assignments"
      and "group_coaching_series" and is comprised of fields such as group_coaching_series_track_assignment_id,
      group_coaching_series_id, track_assignment_id, source_id, source_type, created_at,
      updated_at and revoked_at. [GenAI Description]
    columns:
      - name: group_coaching_series_track_assignment_id
        description: The 'group_coaching_series_track_assignment_id' is a unique identifier
          for a record that links an individual track assignment and a group coaching
          series. [GenAI Description]
        tests:
          - not_null
          - unique

      - name: group_coaching_series_id
        description: The 'group_coaching_series_id' column stores the unique identification
          number of the group coaching series to which a given track assignment belongs.
          [GenAI Description]

      - name: track_assignment_id
        description: The 'track_assignment_id' column of this DBT model stores the
          unique identifier of the track assignment associated with a group coaching
          series. [GenAI Description]

      - name: source_id
        description: The column 'source_id' represents the unique identifier of the
          source of the record. [GenAI Description]

      - name: source_type
        description: The 'source_type' column indicates the source type of each track
          assignment, such as "manual" or "source_tool". [GenAI Description]

      - name: created_at
        description: '"created_at" stores the timestamp of when the record was created,
          updated to reflect the start of the series registration if created after
          the series end date. [GenAI Description]'

      - name: updated_at
        description: The column 'updated_at' stores a timestamp of when the group
          coaching series track assignment was last updated. [GenAI Description]

      - name: revoked_at
        description: The 'revoked_at' column indicates the timestamp at which a track
          assignment was revoked for a group coaching series. [GenAI Description]
