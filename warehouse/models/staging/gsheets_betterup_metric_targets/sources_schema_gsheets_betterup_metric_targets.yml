version: 2

sources:
  - name: gsheets_betterup_metric_targets
    database: raw
    schema: "{{ 'airbyte_gsheets_betterup_metric_targets' if env_var('DEPLOYMENT_ENVIRONMENT', '') == 'US Gov' else 'stitch_gsheets_betterup_metric_targets' }}"
    loader: "{{ 'airbyte' if env_var('DEPLOYMENT_ENVIRONMENT', '') == 'US Gov' else 'stitch' }}"

    tables:
      - name: metric_targets