version: 2

models:
  - name: stg_segment_backend__user_created
    description: The DBT model stg_segment_backend__user_created is a staging model
      created by DBT. It includes fields such as user_id, email, deployment_type,
      platform_name, browser_name, product_subscription_assignment_id, product_subscription_id,
      and product_id. This model also contains relationships to its parent models,
      and is created using raw SQL code. [GenAI Description]
    columns:
      - name: user_id
        description: The 'user_id' column stores a unique identifier for each user
          in the dataset. [GenAI Description]
        tests:
          - unique
          - not_null

      - name: email
        description: The 'email' column stores the email address associated with a
          user. [GenAI Description]

      - name: deployment_type
        description: The 'deployment_type' column contains the method used to deploy
          an application/program. [GenAI Description]

      - name: track_id
        description: The 'track_id' column stores the unique identifier associated
          with an individual user. [GenAI Description]

      - name: track_assignment_id
        description: "'track_assignment_id' is the identifier of a user's current\
          \ track or course. [GenAI Description]"

      - name: created_at
        description: "'created_at' stores a timestamp of when the user was created.\
          \ [GenAI Description]"

      - name: platform_name
        description: This column describes the operating system of the device used
          by the user. [GenAI Description]

      - name: platform_version
        description: This column contains the version numbers of the platforms used
          by users. [GenAI Description]

      - name: device_name
        description: The device_name column contains the name of the device used by
          the user when the event occurred. [GenAI Description]

      - name: browser_name
        description: The 'browser_name' column stores the name of the browser used
          to access the product. [GenAI Description]

      - name: browser_version
        description: "'browser_version' stores the version of the browser used by\
          \ a user. [GenAI Description]"

      - name: app_build
        description: "'app_build' is the build of the application being used. [GenAI\
          \ Description]"

      - name: app_version
        description: The 'app_version' column contains the version of the application
          in use. [GenAI Description]

      - name: product_subscription_assignment_id
        description: The 'product_subscription_assignment_id' column stores the unique
          identifier of a user's assigned subscription product. [GenAI Description]

      - name: product_subscription_id
        description: The product_subscription_id column contains the unique identifier
          of the product subscription associated with the user. [GenAI Description]

      - name: product_id
        description: The 'product_id' column stores the ID of a product associated
          with the user. [GenAI Description]
