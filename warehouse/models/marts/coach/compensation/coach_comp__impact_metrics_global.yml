version: 2

models:
  - name: coach_comp__impact_metrics_global
    description: All metrics combined for coach compensation US + EU

    columns:
      - name: user_season_id
        description: Surrogate Key of User UUID + Season Name. Functions as primary key.
        tests:
          - not_null
          - unique
      - name: user_uuid
        tests:
          - not_null
      - name: us_coach_id
        tests:
          - not_null
      - name: coach_profile_uuid
      - name: full_name
      - name: season_name