version: 2

models:
  - name: dim_customers
    meta:
      owner: "adrian.lievano@betterup.co"
    description: >
      A table tracking all customers in stripe along with app user data.

    columns:
      - name: stripe_customer_id
        description: Primary key
        tests:
          - unique
          - not_null
