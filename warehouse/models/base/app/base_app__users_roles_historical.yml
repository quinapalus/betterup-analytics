version: 2

models:
  - name: base_app__users_roles_historical
    description: ""
    columns:
      - name: user_role_id
        description: primary key generated by surrogate key macro.
        tests:
          - unique
          - not_null
      - name: role_id
      - name: user_id
      - name: created_at
