#!/bin/bash

# configure bash to exit on error
set -e
set -o pipefail

# Create a virtualenv to install the MindTickle tap
python3 -m venv venv

venv/bin/pip install packages/bu-pulse-survey
venv/bin/send-first-pulse-survey full --start='1/18/19' --url=$MODE_DEI_SMI_URL
